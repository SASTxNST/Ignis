{
  "name": "@ignis/physics-engine",
  "version": "0.1.0",
  "description": "Pure TypeScript rocket flight simulation core for Ignis. No UI, no networking — a deterministic function from RocketConfig to SimulationRun.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "validate": "ts-node src/validate.ts",
    "dev": "ts-node-dev --respawn src/validate.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "mathjs": "^15.2.0"
  }
}
